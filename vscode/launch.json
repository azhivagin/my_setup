{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug main",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/main",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C/C++: g++ сборка активного файла"
        }
    ]
}